# 2025 前端跨窗口通信最佳实践（多种方案选择参考）

https://juejin.cn/post/7526419856638148649#heading-4

# Python pip

https://www.cainiaojc.com/python/python-pip.html

# mindsDb

https://docs.mindsdb.com/integrations/data-overview
https://github.com/mindsdb/mindsdb

```sh
pip install mindsdb
```

# 告别重复训练：brain.js 模型 JSON 持久化与迁移学习实战指南

https://blog.csdn.net/gitblog_00806/article/details/153097776

# 深度克隆

https://developer.mozilla.org/zh-CN/docs/Web/API/Window/structuredClone

# 地图适配左右侧栏

```js
onFitView(start, end) {
            this.isAction = true;
            const sideWidth = 340;
            const container = document.getElementById('container');
            const distance = (this.showLeft ? sideWidth : 0) + (this.showRight ? sideWidth : 0);
            const ww = container.offsetWidth - distance;
            const hh = container.offsetHeight;
            const moveCenter = sideWidth * 0.5;
            setTimeout(() => {
                this.isAction = false;
            }, 1000);

            for (let i = 8; i < 18; i++) {
                const p = this.map.project(start, i);
                const p1 = this.map.project(end, i);
                if (Math.abs(p.x - p1.x) >= ww || Math.abs(p.y - p1.y) >= hh) {
                    console.log('fitView', i);
                    const zoom = i - 1;

                    let center = [0.5 * (start[0] + end[0]), 0.5 * (start[1] + end[1])];
                    const p2 = this.map.project(center, zoom);
                    if (this.showLeft && !this.showRight) {
                        center = this.map.unproject([p2.x - moveCenter, p2.y], zoom);
                    } else if (!this.showLeft && this.showRight) {
                        center = this.map.unproject([p2.x + moveCenter, p2.y], zoom);
                    }
                    this.map.setView(center, zoom);
                    console.log('zoom', zoom);
                    this.currentZoom = zoom;
                    return zoom;
                }
            }
        }
```

# 窗口缩放百分比

https://www.zhangxinxu.com/wordpress/2021/02/js-if-page-zoom/

# electron 打开文件所在位置

```js
//打开E盘根目录下名为test的文件夹
const {shell} = require('electron');
shell.openPath('E:\\test');
// 打开当前electron项目根目录下名为src的文件夹
const {app, shell} = require('electron');
shell.openPath(`${app.getAppPath()}\\src`);
```

```js
if (fs.existsSync(filePath)) {
  shell.showItemInFolder(filePath);
}
```

# Node.js 设置定时任务：node-schedule 模块的使用

https://segmentfault.com/a/1190000022455361

# svgtofont svg 转字体

https://github.com/jaywcjlove/svgtofont

# 2025 复盘 | 🚀 穿越 AI 焦虑周期，进化为 "AI 全栈"

https://aicoding.juejin.cn/post/7581648776303755273

# DocumentFragment 性能优化

https://developer.mozilla.org/zh-CN/docs/Web/API/DocumentFragment

DocumentFragment 是一个轻量级的文档节点容器。它不在主文档树中，可以看作是一个“虚拟”的 DOM 片段。我们可以在内存中操作这个容器，最后一次性将其内容插入到真实 DOM 中，从而大幅减少浏览器的重排次数。

先说是什么：DocumentFragment 是一个轻量级的文档节点容器，不属于主文档树，用于临时存储 DOM 节点。
再说为什么：直接操作 DOM 会触发重排，频繁操作影响性能。DocumentFragment 在内存中操作 DOM，最后一次性插入，大幅减少重排次数。
然后说怎么用：通过 document.createDocumentFragment()创建，像操作普通 DOM 一样添加子节点，最后一次性插入到目标位置。
最后说应用场景：批量渲染大量数据、动态构建复杂组件、高效移动 DOM 节点等。

# 收好这份 Python 项目终极打包指南：4 种方法任你选！

https://zhuanlan.zhihu.com/p/1980203002070599193

# Python 实战】5 种方法将 Python 代码打包成 EXE 可执行文件

https://cloud.tencent.com/developer/article/2521095

# Python typing 类型检查

https://docs.python.org/3/library/typing.html

# HTML 转 PDF

## pdfkit

https://www.npmjs.com/package/pdfkit

## pdf-lib

https://www.npmjs.com/package/pdf-lib

## pdfmake

https://www.npmjs.com/package/pdfmake?activeTab=readme

http://pdfmake.org/

https://pdfmake.github.io/docs/0.1/

https://oct1a.github.io/pdfmake-docs/#/

## @react-pdf/renderer

# web 端查看 word

- Mammoth.js

- docx-preview (aka docx-renderer)
- docxjs / docx4js

嵌入 Office Online / OneDrive 查看（最佳原生保真，零转换）

Office Web Viewer / Office Online 外部嵌入
用法（直接把文档托管在可访问 URL）： https://view.officeapps.live.com/op/embed.aspx?src={URL_ENCODED_DOC_URL}

```html
<iframe
  src="https://view.officeapps.live.com/op/embed.aspx?src=https%3A%2F%2Fyourdomain.com%2Ffiles%2Fexample.docx"
  width="100%"
  height="600"
></iframe>
```

react-file-viewer / react-doc-viewer / ngx-doc-viewer
这些组件通常是对上面方案的封装（嵌入 Office Viewer、或将文件转换后使用 PDF.js、或使用 Mammoth 等）。
优点：方便在现有框架中集成；缺点取决于底层实现。

# web 端查看 ppt

纯前端解析 PPTX（理论可行，但有限制）
技术点：pptx 是 OpenXML（zip + XML + media）。可以用 JSZip 读 zip，然后解析 XML，渲染成 HTML/SVG。
已知事实：没有成熟、通用且完备的纯前端“pptx→ 原样渲染”库（创建库如 PptxGenJS 用于生成 PPTX，而非渲染）。
优点：不依赖后端或第三方服务；可实现高度自定义渲染。
缺点：需要自己实现布局、SLIDE 转换、字体、动画、复杂样式等，非常耗工且难以兼容所有 PPT 特性。
结论：仅在你接受对兼容性/样式做妥协并愿意开发时考虑此路。

Microsoft Office Web Viewer

```html
<iframe
  src="https://view.officeapps.live.com/op/view.aspx?src=https%3A%2F%2Fyourdomain.com%2Ffiles%2Fexample.pptx"
  width="100%"
  height="600px"
  frameborder="0"
>
</iframe>
```

# 字体资源

http://www.downcc.com/font/

# Vite 动态引入`unplugin-auto-import`

https://github.com/unplugin/unplugin-auto-import
https://juejin.cn/post/7208099384070815803

# dompdf.js

https://mp.weixin.qq.com/s/Zk4DqM5dV1UtIHKAQO9gwg?scene=1&click_id=2

# 对于模块动态加载，Vite 内部做了哪些优化

https://juejin.cn/post/7438124226501459968

# Zod 请求参数校验

https://zod.nodejs.cn/basics

# TypeORM node 后台数据库对象映射模型

https://typeorm.io/docs/entity/entities

# 腾讯 AlloyTeam 知识库

https://github.com/AlloyTeam/Mars

# 面试官最爱挖的坑：用户 Token 到底该存哪？

https://juejin.cn/post/7583898823920451626

# 【用户行为监控】别只做工具人了！手把手带你写一个前端埋点统计 SDK

https://juejin.cn/post/7583612559443279923

# AI 学习路径

https://github.com/AMAI-GmbH/AI-Expert-Roadmap

# DeepWiki：AI 驱动、免费且实用的 GitHub 源码阅读与分析神器！

https://deepwiki.com/

https://zhuanlan.zhihu.com/p/1902708829713588967

阅读 pinia 源码
https://deepwiki.com/vuejs/pinia/1-overview
