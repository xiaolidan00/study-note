{
  "scripts": {
    "dev": "gitbook serve",
    "build": "gitbook build",
    "install": "gitbook install"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "normalize.css": "^8.0.1",
    "showdown": "^2.1.0"
  }
}
